Question:
Find minimum possible denominations for given valid currency set.

Code:

valid = list(map(int, input("Enter valid currencies (comma separated): ").split(",")))
money = int(input("Enter money: "))
valid.sort(reverse=True)
for v in valid:
    if money >= v:
        cnt = money // v
        print(f"{v}-{cnt}")
        money %= v
Output:
Input 1:

1,2,5,10,20,50,100,200,500,2000
210


Output 1:

200-1
10-1


Input 2:

1,2,5,10,20,50,100,200,500
556


Output 2:

500-1
50-1
5-1
1-1
