Question:
Check if password is valid:

At least 1 lowercase, 1 uppercase, 1 digit, 1 special char ($#@), length 6–12.

Code:

import re
passwords = input("Enter passwords: ").split(",")
valid = []
for p in passwords:
    if (6 <= len(p) <= 12 and
        re.search("[a-z]", p) and
        re.search("[A-Z]", p) and
        re.search("[0-9]", p) and
        re.search("[$#@]", p)):
        valid.append(p)
print(",".join(valid))


Input:

ABd1234@1,a F1#,2w3E*,2We3345


Output:

ABd1234@1
